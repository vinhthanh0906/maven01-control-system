/*
  rover_control_system.ino (Optimized for Low Latency)
  Wemos R32 D1 — AP mode UDP control + DHT11 + Ultrasonic

  Wiring:
  ┌──────────┬───────────┐
  │ L298N    │ Wemos R32 │
  ├──────────┼───────────┤
  │ IN1      │ IO25      │
  │ IN2      │ IO26      │
  │ IN3      │ IO27      │
  │ IN4      │ IO17      │
  │ ENA/ENB  │ jumpered  │
  ├──────────┼───────────┤
  │ DHT11    │           │
  │ VCC      │ 3.3V      │
  │ DATA     │ IO14      │
  │ GND      │ GND       │
  └──────────┴───────────┘
*/

#include <WiFi.h>
#include <WiFiUdp.h>
#include <ArduinoJson.h>
#include <DHT.h>
#include "esp_wifi.h"  // Required for disabling Wi-Fi power save

// ── Ultrasonic Sensor ─────────────────────────────────────────────────────────
const int trigPin = 19;
const int echoPin = 18;
int distance = 0;

// ── Access Point Config ───────────────────────────────────────────────────────
const char* AP_SSID     = "RoverAP";
const char* AP_PASSWORD = "rover1234";

// ── UDP Ports ─────────────────────────────────────────────────────────────────
const int UDP_RECV_PORT = 4210;
const int UDP_SEND_PORT = 4211;

// ── L298N Pins ────────────────────────────────────────────────────────────────
#define IN1  25
#define IN2  26
#define IN3  27
#define IN4  17

// ── DHT11 Config ──────────────────────────────────────────────────────────────
#define DHT_PIN  14
#define DHT_TYPE DHT11
DHT dht(DHT_PIN, DHT_TYPE);

// ── Timing Context (Non-blocking) ─────────────────────────────────────────────
unsigned long lastDHTRead       = 0;
unsigned long lastUltrasonicRead = 0;
unsigned long lastTelemetrySent  = 0;

const unsigned long DHT_INTERVAL        = 2000; // Read DHT every 2s
const unsigned long ULTRASONIC_INTERVAL = 100;  // Read distance every 100ms (10Hz)
const unsigned long TELEMETRY_INTERVAL  = 100;  // Stream telemetry every 100ms (10Hz)

// Cached Sensor States
float cachedTemp     = 0.0;
float cachedHumidity = 0.0;
bool  sensorOK       = false;

// ── System State ──────────────────────────────────────────────────────────────
WiFiUDP   udp;
IPAddress pcIP;
bool      pcKnown    = false;
String    currentCmd   = "stop";
int       currentSpeed = 0;
unsigned long tickCount = 0;

// ── Motor Control Execution ───────────────────────────────────────────────────
void motorStop() {
  digitalWrite(IN1, LOW); digitalWrite(IN2, LOW);
  digitalWrite(IN3, LOW); digitalWrite(IN4, LOW);
}
void motorForward() {
  digitalWrite(IN1, HIGH); digitalWrite(IN2, LOW);
  digitalWrite(IN3, HIGH); digitalWrite(IN4, LOW);
}

void motorBackward() {
  digitalWrite(IN1, LOW);  digitalWrite(IN2, HIGH);
  digitalWrite(IN3, LOW);  digitalWrite(IN4, HIGH);
}

void motorLeft() {
  digitalWrite(IN1, LOW);  digitalWrite(IN2, HIGH);
  digitalWrite(IN3, HIGH); digitalWrite(IN4, LOW);
}

void motorRight() {
  digitalWrite(IN1, HIGH); digitalWrite(IN2, LOW);
  digitalWrite(IN3, LOW);  digitalWrite(IN4, HIGH);
}

void applyCommand(const String& cmd, int spd) {
  currentCmd   = cmd;
  currentSpeed = (cmd == "stop") ? 0 : spd;
  
  if      (cmd == "forward")  motorForward();
  else if (cmd == "backward") motorBackward();
  else if (cmd == "left")     motorLeft();
  else if (cmd == "right")    motorRight();
  else                        motorStop();
}

// ── Sensor Sampling ───────────────────────────────────────────────────────────
void readDHT() {
  float h = dht.readHumidity();
  float t = dht.readTemperature();
  if (isnan(h) || isnan(t)) {
    sensorOK = false;
  } else {
    cachedTemp     = t;
    cachedHumidity = h;
    sensorOK       = true;
  }
}

void readUltrasonic() {
  digitalWrite(trigPin, LOW);
  delayMicroseconds(2);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  digitalWrite(trigPin, LOW);

  // Added a 20000 microsecond timeout (~3.4m maximum range). 
  // This stops the function from freezing the CPU if no reflection is caught.
  long duration = pulseIn(echoPin, HIGH, 20000); 
  
  if (duration == 0) {
    distance = 999; // Clear path or out of range
  } else {
    distance = duration * 0.034 / 2;
  }
}

// ── Asynchronous Telemetry Delivery ──────────────────────────────────────────
void sendTelemetry() {
  if (!pcKnown) return;

  tickCount++;
  StaticJsonDocument<256> doc;
  doc["status"]      = "ok";
  doc["cmd"]         = currentCmd;
  doc["speed"]       = currentSpeed;
  doc["uptime"]      = millis() / 1000;
  doc["tick"]        = tickCount;
  doc["distance"]    = distance;
  doc["temperature"] = sensorOK ? round(cachedTemp * 10) / 10.0     : 0;
  doc["humidity"]    = sensorOK ? round(cachedHumidity * 10) / 10.0 : 0;
  doc["sensor_ok"]   = sensorOK;

  char buf[256];
  serializeJson(doc, buf);

  udp.beginPacket(pcIP, UDP_SEND_PORT);
  udp.print(buf);
  udp.endPacket();
}

// ── Setup ─────────────────────────────────────────────────────────────────────
void setup() {
  // High-speed baud rate prevents UART transmission serialization delays from stalling execution
  Serial.begin(921600); 
  delay(500);
  Serial.println("\n=== High-Performance Rover System ===");

  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
pinMode(IN1, OUTPUT); pinMode(IN2, OUTPUT);
  pinMode(IN3, OUTPUT); pinMode(IN4, OUTPUT);
  motorStop();

  dht.begin();

  // Initialize Wireless Hardware
  WiFi.mode(WIFI_AP);
  WiFi.softAP(AP_SSID, AP_PASSWORD);
  
  // CRITICAL FOR LATENCY: Turn off modem power-saving features completely
  WiFi.setSleep(false);
  esp_wifi_set_ps(WIFI_PS_NONE); 

  udp.begin(UDP_RECV_PORT);
  Serial.println("[READY] System operational loop running.");
}

// ── Core Execution Loop ───────────────────────────────────────────────────────
void loop() {
  unsigned long now = millis();

  // 1. Instantly Check and Parse Command Packets
  int packetSize = udp.parsePacket();
  if (packetSize > 0) {
    char buf[128];
    int len = udp.read(buf, sizeof(buf) - 1);
    if (len > 0) {
      buf[len] = '\0';
      pcIP    = udp.remoteIP();
      pcKnown = true;

      StaticJsonDocument<128> doc;
      DeserializationError err = deserializeJson(doc, buf);
      if (!err) {
        const char* cmd = doc["cmd"] | "stop";
        int speed       = doc["speed"] | 150;
        applyCommand(String(cmd), speed);
      }
    }
  }

  // 2. Sample Ultrasonic Sensor (Asynchronous - 10Hz)
  if (now - lastUltrasonicRead >= ULTRASONIC_INTERVAL) {
    lastUltrasonicRead = now;
    readUltrasonic();
  }

  // 3. Stream Telemetry Data Back to Python Host (Asynchronous - 10Hz)
  if (now - lastTelemetrySent >= TELEMETRY_INTERVAL) {
    lastTelemetrySent = now;
    sendTelemetry();
  }

  // 4. Sample Ambient Environmental Conditions (Asynchronous - 0.5Hz)
  if (now - lastDHTRead >= DHT_INTERVAL) {
    lastDHTRead = now;
    readDHT();
  }
}